apiVersion: v1
kind: Service
metadata:
  name: taskscale-worker
  namespace: taskscale
  labels:
    app: taskscale-worker
spec:
  selector:
    app: taskscale-worker
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP