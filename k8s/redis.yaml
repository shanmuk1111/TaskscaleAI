apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskscale-redis
  namespace: taskscale
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskscale-redis
  template:
    metadata:
      labels:
        app: taskscale-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: taskscale
spec:
  selector:
    app: taskscale-redis
  ports:
    - port: 6379
      targetPort: 6379