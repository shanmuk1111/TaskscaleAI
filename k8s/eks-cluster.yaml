apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: taskscale-cluster
  region: eu-north-1
  version: "1.36"

managedNodeGroups:
  - name: taskscale-workers
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 3